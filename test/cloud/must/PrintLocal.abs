module PrintLocal;

import * from LocalDC;

interface IPrint {
    Unit print(String b);
}

class Print implements IPrint {
    {
      println("print is up");
    }
    Unit print(String b) {
        println(concatenate("remote has sent", b));
    }
}


{
  println("mplo");
  IDC dc = new LocalDC(9001);
  IPrint o = dc spawns Print();
  o ! print("hello");
}