module ExceptionMod;

exception MyException(Int, Bool);

{
Int x=0;

try {
    x = x + 1;
    throw MyException(x, True);
    x = x + 10;
   }
catch {
   MyException(0,False) => println(toString(False));
   MyException(1,False) => println(toString(False));
   MyException(1,True) => {x=x+1; x = x+2;}
}
finally {
   x=x+1;
   println(toString(x==5));
}

}