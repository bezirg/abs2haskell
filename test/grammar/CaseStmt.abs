module CaseStmt;

{
 Maybe<Int> x = Nothing;
 case x {
     // Nothing  => 3; // broken, confused between case-exp and case-stmt
     Just(_) => println("mplo");
 }
 case x {
     Just(_) => println("False");
     Nothing => println("True");
  }
}